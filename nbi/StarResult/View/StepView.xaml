<Page x:Class="StarResult.View.StepView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StarResult.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StepView">

    <Grid>
        <ListView ItemsSource="{Binding StepModels}" 
                              Background="#FFE6EFCE" Foreground="#ff101010"
                              PreviewMouseLeftButtonUp="StepList_PreviewMouseLeftButtonUp" >
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}">
                    <Setter Property="BorderBrush" Value="DarkGray" />
                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsContact}" Value="true">
                            <Setter Property="Background" Value="#FF7090D0" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsContact}" Value="false">
                            <Setter Property="Background" Value="#FF80D0A0" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView >
                    <GridViewColumn Width="80" Header="Step"  DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Width="126" Header="Start" DisplayMemberBinding="{Binding Start}"/>
                    <GridViewColumn Width="126" Header="End"   DisplayMemberBinding="{Binding End}"/>
                    <GridViewColumn Width="100" Header="Yeild" DisplayMemberBinding="{Binding Yeild}" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Page>
