<Page x:Class="Debug_TempCont.View.GraphView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Debug_TempCont.View"
      xmlns:vm="clr-namespace:Debug_TempCont.ViewModel" 
      xmlns:sp="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="570" d:DesignWidth="800"
      Title="GraphView">

    <Page.DataContext>
        <vm:GraphVM/>
    </Page.DataContext>
    
    <Page.Resources>
        
        <Style x:Key="checkBoxStyleTrigger" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <Border x:Name="Border12" Margin="1" BorderThickness="0" Background="#f3e5f5" Padding="30, 10" CornerRadius="10" >
                            <TextBlock x:Name="TextContent" HorizontalAlignment="Center" Text="{TemplateBinding Content}" FontSize="20" FontWeight="Bold" Foreground="Black" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="CheckBox.IsChecked" Value="True">
                                <Setter TargetName="Border12" Property="Background" Value="#6200ea"/>
                                <Setter TargetName="TextContent" Property="Foreground" Value="white"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <SolidColorBrush x:Key="PrimaryBackgroundColor" Color="#496fff"/>
        <SolidColorBrush x:Key="SecundaryBackgroundColor" Color="#ffffff"/>

        <SolidColorBrush x:Key="PrimaryBlueColor" Color="#263faa"/>
        <SolidColorBrush x:Key="PrimaryGrayColor" Color="#64677a"/>

        <SolidColorBrush x:Key="SecundaryBlueColor" Color="#496fff"/>
        <SolidColorBrush x:Key="SecundaryGraytColor" Color="#cececece"/>

        <!-- ToggleButtonStyle -->
        <Style x:Key="ToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryGrayColor}" />
            <Setter Property="Height" Value="10" />
            <Setter Property="Width" Value="25" />
            <Setter Property="MaxHeight" Value="10" />
            <Setter Property="MaxWidth" Value="25" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Grid>
                            <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="10"
                                    Height="10" Width="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
                            <Ellipse x:Name="Ellipse" Height="12" Width="12"
                                     VerticalAlignment="Center" HorizontalAlignment="Left" Fill="{DynamicResource SecundaryBackgroundColor}"
                                     Stroke="{TemplateBinding BorderBrush}" StrokeThickness="1.5"/>
                        </Grid>
                        <ControlTemplate.Triggers>

                            <EventTrigger RoutedEvent="Unchecked">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="Border"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#cecece" Duration="0:0:0.2" />
                                        <ThicknessAnimation Storyboard.TargetName="Ellipse"
                                                            Storyboard.TargetProperty="Margin"
                                                            To="0 0 0 0" Duration="0:0:0.2" />

                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Checked">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="Border"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#c2d1fc" Duration="0:0:0.2" />

                                        <ThicknessAnimation Storyboard.TargetName="Ellipse"
                                                            Storyboard.TargetProperty="Margin"
                                                            To="10 0 0 0" Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Ellipse" Property="Fill"
                                        Value="{DynamicResource SecundaryBlueColor}" />
                                <Setter TargetName="Ellipse" Property="Stroke"
                                        Value="{DynamicResource PrimaryBlueColor}" />
                            </Trigger>
                            
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- ToggleButtonStyle -->
    </Page.Resources>
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0 0.1" EndPoint="0 1">
                <LinearGradientBrush.GradientStops>
                    <GradientStop Offset="0.0" Color="#fafafa" />
                    <GradientStop Offset="1.0" Color="#212121" />
                </LinearGradientBrush.GradientStops>
            </LinearGradientBrush>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="55"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <CheckBox Content="AutoFit" Style="{StaticResource checkBoxStyleTrigger}" IsChecked="{Binding AutoFit}" Margin="5" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Grid Grid.Row="0" Grid.Column="1">
            <ItemsControl ItemsSource="{Binding GraphCHs}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ToolBar Header="{Binding Grpahch}" Height="25" Width="100" Margin="0" VerticalAlignment="Center" HorizontalAlignment="Center" Background="#FFF3F5F1"  Foreground="{Binding ColorT}" FontFamily="Courier New" FontSize="15" FontWeight="Bold">
                            <ToggleButton VerticalAlignment="Center" HorizontalAlignment="Center" Style="{DynamicResource ToggleButtonStyle}" IsChecked="{Binding Show}"/>
                        </ToolBar>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Grid.Row" Value="{Binding GrpahRow}" />
                        <Setter Property="Grid.Column" Value="{Binding GrpahCol}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Grid>
        <sp:WpfPlot Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10,10,10,10" x:Name="xTempGraph"/>
    </Grid>
</Page>
