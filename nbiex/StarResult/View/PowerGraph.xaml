<Page x:Class="StarResult.View.PowerGraph"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StarResult.View"
      xmlns:sp="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
      xmlns:vm="clr-namespace:StarResult.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PowerGraph">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ItemsControl Grid.Column="0" ItemsSource="{Binding DPSs}" Margin="3,3,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Width" Value="35" />
                                    <Setter Property="TextAlignment" Value="Center" />
                                </Style>
                            </StackPanel.Resources>
                            <Border BorderBrush="{Binding ColorV}" CornerRadius="3" BorderThickness="1">
                                <TextBlock Text="{Binding Name}" Foreground="{Binding ColorC}" FontSize="10"/>
                            </Border>
                            <CheckBox x:Name="chkDPS" IsChecked="{Binding Show, UpdateSourceTrigger=PropertyChanged}" Width="30" Margin="4,0,0,0" Background="DarkSlateGray" Click="CheckBox_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl Grid.Column="1" ItemsSource="{Binding HVs}" Margin="3,3,0,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Width" Value="35" />
                                    <Setter Property="TextAlignment" Value="Center" />
                                </Style>
                            </StackPanel.Resources>
                            <Border BorderBrush="{Binding ColorV}" CornerRadius="3" BorderThickness="1">
                                <TextBlock Text="{Binding Name}" Foreground="{Binding ColorC}" FontSize="10"/>
                            </Border>
                            <CheckBox x:Name="chkHV" IsChecked="{Binding Show, UpdateSourceTrigger=PropertyChanged}" Width="30" Margin="4,0,0,0" Background="DarkSlateGray" Click="CheckBox_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <!--
            <ItemsControl Grid.Column="1" ItemsSource="{Binding DPS2s}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Width" Value="35" />
                                    <Setter Property="TextAlignment" Value="Center" />
                                </Style>
                            </StackPanel.Resources>
                            <Border BorderBrush="{Binding ColorV}" CornerRadius="3" BorderThickness="1">
                                <TextBlock Text="{Binding Name}" Foreground="{Binding ColorC}"/>
                            </Border>
                            <CheckBox IsChecked="{Binding Show}" Width="30" Margin="4,0,0,0" Background="DarkSlateGray"  Click="CheckBox_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            -->

        </Grid>
        <sp:WpfPlot Grid.Column="1" x:Name="xPowerGraph" />
    </Grid>
</Page>
