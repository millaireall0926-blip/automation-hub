<Page x:Class="StarIDE.View.TerminalView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:t="clr-namespace:Simple.Wpf.Terminal;assembly=Simple.Wpf.Terminal"
      xmlns:vm="clr-namespace:StarIDE.ViewModel"    
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:local="clr-namespace:StarIDE.View" xmlns:hc="https://handyorg.github.io/handycontrol"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TerminalView">

    <Page.DataContext>
        <vm:TermnalVM/>
    </Page.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Slot IP" FontSize="14" Foreground="White" FontWeight="Bold" VerticalAlignment="Center" Margin="10,2,0,2"/>
                <ComboBox ItemsSource="{Binding SlotIPs}" VerticalAlignment="top" SelectedValue="{Binding SelectIP}" Margin="5,0,0,0" Width="60"/>
                <Button Content="RUN" IsEnabled="{Binding EnableCmd}" Command="{Binding CmdRun}"  Width="50" Background="DarkSeaGreen" Margin="5,4,0,4"/>
                <Button Content="Copy" IsEnabled="{Binding EnableCmd}" Command="{Binding CmdCopy}" Width="50" Background="DarkSlateBlue"  Margin="5,4,0,4"/>
                <Button Content="Ping" IsEnabled="{Binding EnableCmd}" Command="{Binding CmdPing}" Width="50" Background="Orange"  Margin="5,4,0,4"/>
                <Button Content="Cancel" FontSize="10" IsEnabled="{Binding EnableCancle}" Command="{Binding CmdCancle}" Width="50" Background="IndianRed"  Margin="5,4,0,4"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <CheckBox Content="Net" IsChecked="{Binding UseNet}" Margin="0,0,5,0" />
                <Button Content="{Binding ExeMode}" Command="{Binding CmdMode}" Width="60" Margin="0,0,5,0" />
                <TextBlock Text="Diag Arg(Hex)" VerticalAlignment="Center" />
                <TextBox Text="{Binding DiagArg}" Width="40"></TextBox>
            </StackPanel>
            
        </Grid>
        <Grid Grid.Row="1">
            <t:Terminal x:Name="TerminalOutput"
                IsReadOnlyCaretVisible="False"
                VerticalScrollBarVisibility="Visible"
                HorizontalScrollBarVisibility="Visible"
                IsReadOnly="false"
                Prompt=">"
                AutoScroll="True"
                ItemHeight="1"
                ItemsSource="{Binding Path=Lines, Mode=OneWay}"
                Background="Black" Foreground="White" >

                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="LineEntered">
                        <b:InvokeCommandAction Command="{Binding Path=CmdInput, Mode=OneWay}"
                                       CommandParameter="{Binding Path=Line, Mode=OneWay, ElementName=TerminalOutput}" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>

            </t:Terminal>


            <hc:LoadingCircle Grid.Row="1" Visibility="{Binding CmdWait}" />

        </Grid>
    </Grid>
</Page>

    