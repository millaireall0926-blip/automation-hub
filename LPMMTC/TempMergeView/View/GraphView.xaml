<Page x:Class="TempMergeView.ViewModel.GraphView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TempMergeView.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:vm="clr-namespace:TempMergeView.ViewModel"        
      xmlns:hc="https://handyorg.github.io/handycontrol"      
      xmlns:sp="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
      xmlns:model="clr-namespace:TempMergeView.Model"
      Title="GraphView">

    <Page.DataContext>
        <vm:GraphVM />
    </Page.DataContext>
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="160"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,0,5,0">
                <TextBlock Text="ALL " Foreground="Black" VerticalAlignment="Center"></TextBlock>
                <ToggleButton Margin="2,0,0,0"  Style="{StaticResource ToggleButtonSwitch}" IsChecked="True"  Click="ToggleDimmAll_Click" />
            </StackPanel>
            <ItemsControl Grid.Row="1" ItemsSource="{Binding LPDatas}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5,0,5,0">
                            <TextBlock Text="{Binding Name}" Foreground="{Binding ChanelColor}" VerticalAlignment="Center"></TextBlock>
                            <ToggleButton Uid="{Binding Name}" Margin="2,0,0,0" IsChecked="{Binding Show}" 
                                                          Style="{StaticResource ToggleButtonSwitch}" Command="{Binding DGToggle}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        
        <sp:WpfPlot Grid.Column="1" x:Name="xDimGraph" MouseMove="xDimGraph_MouseMove"></sp:WpfPlot>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,5,0">
                    <TextBlock Text="ALL " Foreground="Black" VerticalAlignment="Center"></TextBlock>
                    <ToggleButton Margin="2,0,0,0"  Style="{StaticResource ToggleButtonSwitch}" IsChecked="True"  Click="ToggleSVAll_Click" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,5,0">
                    <TextBlock Text="ALL " Foreground="Black" VerticalAlignment="Center"></TextBlock>
                    <ToggleButton Margin="2,0,0,0"  Style="{StaticResource ToggleButtonSwitch}" IsChecked="True"  Click="TogglePVAll_Click" />
                </StackPanel>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ItemsControl ItemsSource="{Binding SVDatas}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5,0,5,0">
                                <TextBlock Text="{Binding Name}" Foreground="{Binding ChanelColor}" VerticalAlignment="Center"></TextBlock>
                                <ToggleButton Uid="{Binding Name}" Margin="2,0,0,0" IsChecked="{Binding Show}" 
                                              Style="{StaticResource ToggleButtonSwitch}" Command="{Binding DGToggle}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <ItemsControl Grid.Column="1" ItemsSource="{Binding PVDatas}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5,0,5,0">
                                <TextBlock Text="{Binding Name}" Foreground="{Binding ChanelColor}" VerticalAlignment="Center"></TextBlock>
                                <ToggleButton Uid="{Binding Name}" Margin="2,0,0,0" IsChecked="{Binding Show}" 
                              Style="{StaticResource ToggleButtonSwitch}" Command="{Binding DGToggle}" />
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Grid>
    </Grid>
</Page> 
