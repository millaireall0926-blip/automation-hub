<Page x:Class="TempView.ViewModel.GraphView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TempView.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:vm="clr-namespace:TempView.ViewModel"        
      xmlns:hc="https://handyorg.github.io/handycontrol"      
      xmlns:sp="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
      xmlns:model="clr-namespace:TempView.Model"
      Title="GraphView">

    <Page.DataContext>
        <vm:GraphVM />
    </Page.DataContext>
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <ItemsControl Grid.Row="0" ItemsSource="{Binding LPDatas}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5,0,5,0">
                            <TextBlock Text="{Binding Name}" Foreground="{Binding ChanelColor}" VerticalAlignment="Center"></TextBlock>
                            <ToggleButton Uid="{Binding Name}" Margin="2,0,0,0" IsChecked="{Binding Show}" 
                                                          Style="{StaticResource ToggleButtonSwitch}" Command="{Binding DGToggle}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <ItemsControl Grid.Row="1" ItemsSource="{Binding SVDatas}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5,0,5,0">
                            <TextBlock Text="{Binding Name}" Foreground="{Binding ChanelColor}" VerticalAlignment="Center"></TextBlock>
                            <ToggleButton Uid="{Binding Name}" Margin="2,0,0,0" IsChecked="{Binding Show}" 
                              Style="{StaticResource ToggleButtonSwitch}" Command="{Binding DGToggle}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <ItemsControl Grid.Row="2" ItemsSource="{Binding PVDatas}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5,0,5,0">
                            <TextBlock Text="{Binding Name}" Foreground="{Binding ChanelColor}" VerticalAlignment="Center"></TextBlock>
                            <ToggleButton Uid="{Binding Name}" Margin="2,0,0,0" IsChecked="{Binding Show}" 
              Style="{StaticResource ToggleButtonSwitch}" Command="{Binding DGToggle}" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <sp:WpfPlot Grid.Column="1" x:Name="xDimGraph" MouseMove="xDimGraph_MouseMove"></sp:WpfPlot>
    </Grid>
</Page> 
